#include <stdio.h>

void display(int num) {
    printf("%d\n", num);
}

int main() {
    display(5);
    display(10);
    display(12);
    return 0;
}

// WAP to perform addition of two numbers using function. Name of the function should be add().